# 🚀 Quick Start - การตั้งค่า MongoDB แบบด่วน

## วิธีที่ 1: ใช้ MongoDB Atlas (แนะนำ - ฟรี)

### ขั้นตอนที่ 1: สร้าง MongoDB Atlas Account

1. ไปที่ https://www.mongodb.com/cloud/atlas/register
2. สมัครฟรี (Sign up with Google/Email)
3. เลือก **FREE** tier (M0)

### ขั้นตอนที่ 2: สร้าง Database

1. คลิก **"Build a Database"**
2. เลือก **"M0 FREE"**
3. เลือก Region: **Singapore (AWS)**
4. คลิก **"Create"**

### ขั้นตอนที่ 3: ตั้งค่า Database User

1. ระบบจะถาม Username และ Password
2. **จำ Username และ Password ให้ดี!**
3. คลิก **"Create User"**

### ขั้นตอนที่ 4: ตั้งค่า Network Access

1. เลือก **"My Local Environment"**
2. คลิก **"Add My Current IP Address"** 
   หรือคลิก **"Allow Access from Anywhere"** (0.0.0.0/0)
3. คลิก **"Finish and Close"**

### ขั้นตอนที่ 5: รับ Connection String

1. คลิกปุ่ม **"Connect"** ที่ Cluster ของคุณ
2. เลือก **"Drivers"** 
3. คัดลอก Connection String ที่แสดง:
   ```
   mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
   ```

### ขั้นตอนที่ 6: ตั้งค่าโปรเจค

1. เปิดไฟล์ `.env.local` ในโฟลเดอร์หลักของโปรเจค
2. แก้ไขบรรทัด `MONGODB_URI`:
   ```env
   MONGODB_URI=mongodb+srv://your-username:your-password@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
   ```
3. **แทนที่:**
   - `your-username` → Username ที่สร้างไว้
   - `your-password` → Password ที่สร้างไว้
4. บันทึกไฟล์

### ขั้นตอนที่ 7: ทดสอบการเชื่อมต่อ

เปิด Terminal และรันคำสั่ง:

```bash
node scripts/test-mongodb-connection.js
```

**ผลลัพธ์ที่คาดหวัง:**
```
✅ เชื่อมต่อ MongoDB สำเร็จ!
✅ Ping MongoDB สำเร็จ!
🎉 การตั้งค่าฐานข้อมูลเสร็จสมบูรณ์!
```

### ขั้นตอนที่ 8: เริ่มใช้งาน

```bash
npm run dev
```

เปิดเบราว์เซอร์: http://localhost:3000

---

## วิธีที่ 2: ใช้ MongoDB Local (สำหรับผู้ที่ติดตั้ง MongoDB ไว้แล้ว)

### ติดตั้ง MongoDB Community Edition

1. ดาวน์โหลดจาก: https://www.mongodb.com/try/download/community
2. ติดตั้งตามขั้นตอน
3. รัน MongoDB Service

### ตั้งค่าโปรเจค

แก้ไขไฟล์ `.env.local`:
```env
MONGODB_URI=mongodb://localhost:27017
MONGODB_DATABASE=management_app
```

### ทดสอบและรัน

```bash
node scripts/test-mongodb-connection.js
npm run dev
```

---

## 🔧 แก้ไขปัญหาที่พบบ่อย

### ❌ "Cannot read properties of null (reading 'db')"

**สาเหตุ:** ไม่มี MongoDB URI หรือไม่ถูกต้อง

**วิธีแก้:**
1. ตรวจสอบว่ามีไฟล์ `.env.local` หรือไม่
2. ตรวจสอบว่า `MONGODB_URI` ถูกต้อง
3. **รีสตาร์ทเซิร์ฟเวอร์** (`Ctrl+C` แล้วรัน `npm run dev` ใหม่)

### ❌ "MongoServerError: bad auth"

**สาเหตุ:** Username หรือ Password ผิด

**วิธีแก้:**
1. ตรวจสอบ Username/Password ใน MongoDB Atlas
2. อย่าลืมแทนที่ `<password>` ใน Connection String
3. ถ้า password มีอักขระพิเศษ ให้ encode ก่อน

### ❌ "MongoNetworkError: connect ETIMEDOUT"

**สาเหตุ:** Network Access ไม่ได้ตั้งค่า

**วิธีแก้:**
1. ไปที่ MongoDB Atlas → Network Access
2. เพิ่ม IP Address ของคุณ
3. หรือเลือก "Allow Access from Anywhere"

---

## 📞 ต้องการความช่วยเหลือ?

หากยังพบปัญหา:
1. อ่าน `MONGODB_SETUP.md` เพื่อดูรายละเอียดเพิ่มเติม
2. ตรวจสอบ error log ใน Terminal
3. ดู MongoDB Atlas Logs

---

## ✅ Checklist ก่อนเริ่มใช้งาน

- [ ] สร้าง MongoDB Atlas Account
- [ ] สร้าง Database Cluster (M0 FREE)
- [ ] ตั้งค่า Database User (Username + Password)
- [ ] ตั้งค่า Network Access (Allow IP)
- [ ] คัดลอก Connection String
- [ ] แก้ไขไฟล์ `.env.local`
- [ ] แทนที่ Username และ Password
- [ ] รัน `node scripts/test-mongodb-connection.js`
- [ ] เห็นข้อความ ✅ เชื่อมต่อสำเร็จ
- [ ] รัน `npm run dev`
- [ ] เปิดเบราว์เซอร์ http://localhost:3000

**สำเร็จแล้ว! เริ่มใช้งานได้เลย! 🎉**

