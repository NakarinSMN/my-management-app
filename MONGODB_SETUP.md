# 🗄️ คู่มือการตั้งค่า MongoDB สำหรับระบบผ่อนประกัน

## 📋 ขั้นตอนการตั้งค่า

### 1. สร้างบัญชี MongoDB Atlas (ฟรี)

1. ไปที่ https://www.mongodb.com/cloud/atlas
2. คลิก "Try Free" หรือ "Sign Up"
3. สร้างบัญชีด้วย Google, Email หรือ GitHub

### 2. สร้าง Cluster

1. หลังจาก Login แล้ว คลิก "Build a Database"
2. เลือก **FREE** (M0 Sandbox)
3. เลือก Region ที่ใกล้ที่สุด (แนะนำ: Singapore)
4. ตั้งชื่อ Cluster (หรือใช้ชื่อเริ่มต้น)
5. คลิก "Create"

### 3. ตั้งค่า Database Access

1. ไปที่ **Database Access** ในเมนูซ้าย
2. คลิก "Add New Database User"
3. เลือก "Password" authentication
4. ตั้ง Username และ Password (จดไว้!)
5. Database User Privileges: เลือก "Read and write to any database"
6. คลิก "Add User"

### 4. ตั้งค่า Network Access

1. ไปที่ **Network Access** ในเมนูซ้าย
2. คลิก "Add IP Address"
3. เลือก "Allow Access from Anywhere" (0.0.0.0/0)
4. คลิก "Confirm"

### 5. รับ Connection String

1. กลับไปที่ **Database** ในเมนูซ้าย
2. คลิกปุ่ม "Connect" ที่ Cluster ของคุณ
3. เลือก "Connect your application"
4. เลือก Driver: **Node.js** และ Version: **5.5 or later**
5. คัดลอก Connection String (จะเป็นแบบนี้):
   ```
   mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
   ```

### 6. ตั้งค่าไฟล์ .env.local

1. คัดลอกไฟล์ `.env.local.example` เป็น `.env.local`:
   ```bash
   copy .env.local.example .env.local
   ```

2. แก้ไขไฟล์ `.env.local`:
   ```env
   MONGODB_URI=mongodb+srv://your-username:your-password@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
   MONGODB_DATABASE=management_app
   ```

3. แทนที่:
   - `your-username` → Username ที่สร้างไว้ใน Database Access
   - `your-password` → Password ที่สร้างไว้ใน Database Access

### 7. ทดสอบการเชื่อมต่อ

รันคำสั่ง:
```bash
node scripts/test-mongodb-connection.js
```

ถ้าสำเร็จจะเห็น:
```
✅ เชื่อมต่อ MongoDB สำเร็จ!
✅ Ping MongoDB สำเร็จ!
📋 Collections ที่มีในฐานข้อมูล:
   - customers
   - installment_insurance
🎉 การตั้งค่าฐานข้อมูลเสร็จสมบูรณ์!
```

### 8. เพิ่มข้อมูลตัวอย่าง (ถ้าต้องการ)

รันคำสั่ง:
```bash
node scripts/init-installment-insurance-db.js
```

### 9. รันโปรเจค

```bash
npm run dev
```

เปิดเบราว์เซอร์ไปที่: http://localhost:3000

## 🔧 การแก้ไขปัญหา

### ❌ ข้อผิดพลาด: "MongoServerError: bad auth"

**สาเหตุ:** Username หรือ Password ไม่ถูกต้อง

**วิธีแก้:**
1. ตรวจสอบ Username และ Password ใน Database Access
2. ตรวจสอบว่าคัดลอก Connection String ถูกต้อง
3. ตรวจสอบว่าไม่มีช่องว่างหรืออักขระพิเศษใน Password

### ❌ ข้อผิดพลาด: "MongoServerError: IP not allowed"

**สาเหตุ:** IP Address ไม่ได้รับอนุญาตใน Network Access

**วิธีแก้:**
1. ไปที่ Network Access
2. เพิ่ม IP Address ของคุณ หรือเลือก "Allow Access from Anywhere"

### ❌ ข้อผิดพลาด: "Cannot connect to MongoDB"

**สาเหตุ:** ปัญหาเครือข่ายหรือ Firewall

**วิธีแก้:**
1. ตรวจสอบอินเทอร์เน็ต
2. ปิด VPN หรือ Proxy (ถ้ามี)
3. ตรวจสอบ Firewall ว่าไม่บล็อก MongoDB

## 📚 ข้อมูลเพิ่มเติม

- [MongoDB Atlas Documentation](https://docs.atlas.mongodb.com/)
- [MongoDB Node.js Driver](https://www.mongodb.com/docs/drivers/node/current/)

## 🆘 ต้องการความช่วยเหลือ?

หากพบปัญหา สามารถ:
1. ตรวจสอบ log ใน Terminal
2. ดู MongoDB Atlas Metrics & Logs
3. ติดต่อทีมพัฒนา

